spring:
  datasource:
    url: jdbc:mysql://localhost:3307/bulletin_board
    username: root
    password: 2534
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace

security:
  basic:
    enabled: false