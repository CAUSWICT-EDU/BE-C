아쉬운 점
1. sql에서 table을 좀 더 세분화하고 값을 받아왔으면 좋았을 것 같은데, db연결이 익숙치 않아서 일단 도메인을 통으로 갖다 박은점.
2. JPA에 대해서 잘 몰라서, 유용한 기능들을 활용하지 못한 점.
3. 패키지 구조화에 대해서 이해가 잘 안가서, 제대로 분리를 못한 점.
4. DAO, DTO 이런 개념들도 미숙해서, 어떻게 찾아보면서 하긴 했는데 잘 된건지 모르겠다.
5. 사실 db 제대로 할거였으면 필수/일반 과목 기준 값 테이블도 있어야 하지 않나? 싶긴한데, 현생이슈로 거기까지는 안했습니다
6. UI 제대로 할거면 출력 클래스 따로 만들고 해야 하겠지만, 중하지 않다 생각해서 실행 부분은 그냥 대충 만듦
7. 과목 추가 확장성에 대해서, 제대로 안된 것 같아 아쉬운,,, 일단 과목이 몇개 들어갈 지 상수로 정하려고 했는데,
그럼 db에서부터 받아오면서 course를 다 분리하고 course title, point 컬럼 생성하고 여러모로 복잡해질 것 같아서
일단은 그냥 그 부분에 대해서는 직관적으로 구현을 했습니다


** 궁금한점
DAO가 DB랑 직접 연결을 하는거고, DTO가 연결된 값들을 getter/setter 이용해서 전달 하는거라고 알고 있는데,
그러면 실제 어떤 기능들을 구현할 때는, DTO 객체를 추가해서 거기서 값을 바로 가져오는건지,
아니면, 클래스에 따로 다시 변수 생성하고, DTO 값을 다시 할당한 다음에,
서비스끼리에서 값을 주고 받을 때는 dto가 끼어들지 않고 재할당된 변수로만 주고 받는건지.. 모르겠다.

** 궁금한 점
DAO와 DTO와의 연결, 그리고 실제 사용할 클래스에서 DTO 값을 받아올 때,
그 객체가 그 테이블 그 record인지 어떻게 알고 연결할지 고민하다가,
일단 main에서 dao를 실행시키고, 거기서 dto 배열을 받아와서 실제 기능 구현하는 class에 하나씩 넘겨주는 방식으로 했는데,
좀 더 일반적으로 사용 되는 방식이 있는지 궁금하다.

혹은 내가 지금 가지고 있는 모든 고민들이, spring과 jpa를 사용하면 어느정도 일반론으로 들어가서
해결이 되는 부분인건지..도 잘 모르겠다.


++
추가적으로 DB 연결할 때, DB 브라우저에서 추가를 했는데, 삭제를 어떻게 하는지 모르겠습니다..
(connection 날리는 법)
mySQL workbench에서 db를 아예 날렸는데도 안사라지네요
+ 특정 스키마만 불러오는 방법도 있나요?